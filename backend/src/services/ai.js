// AI summary generator
// TODO: integrate freemodel.dev API

async function generateSummary(quake) {
  // Placeholder
  return `Gempa magnitudo ${quake.magnitude} di ${quake.location || 'lokasi tidak diketahui'}`
}

module.exports = { generateSummary }
